{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --watch --setupFiles dotenv/config",
    "coveralls": "jest --coverage && cat ./coverage/lcov.info | coveralls",
    "start": "node server.js dotenv/config",
    "dev": "nodemon src/server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "jsforce-patterns": "^1.1.1",
    "supertest": "^6.1.3"
  },
  "devDependencies": {
    "coveralls": "^3.1.0",
    "dotenv": "^8.2.0",
    "jest": "^26.6.3",
    "nodemon": "^2.0.7"
  }
}
